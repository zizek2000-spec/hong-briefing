(function(){'use strict';function title(card){return(card.querySelector('h2,h3,a')?.textContent||'').trim()}function key(card){return'daily-briefing-status:'+title(card).slice(0,100)}function install(){document.querySelectorAll('.card').forEach(card=>{const actions=card.querySelector('.card-actions');if(!actions||actions.querySelector('.discard'))return;const btn=document.createElement('button');btn.type='button';btn.className='card-action discard';btn.textContent='폐기';btn.setAttribute('aria-label',`${title(card)} 폐기 표시`);btn.addEventListener('click',()=>{const status=actions.querySelector('.status');if(status){status.dataset.state='폐기';status.textContent='폐기'}localStorage.setItem(key(card),'폐기')});actions.appendChild(btn)})}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>setTimeout(install,0))}else{setTimeout(install,0)}})();